# Changelog

## 2026-08-13

### Added

- Added `POST /api/knowledge/provider/runtime-verification`, an explicit
  RAGFlow embedding/retrieval acceptance probe. It uses a fixed tiny query and
  one already processed source, returns only `verified`/`failed`, `checkedAt`
  and a redacted reason, and never uploads a file, creates a dataset, or
  schedules parsing.
- The 5180 Knowledge bookshelf now verifies RAGFlow before creating a new
  provider-backed document. A failed runtime check stops the upload with an
  actionable safe message, preventing new material from joining a known-broken
  parsing queue.

## Unreleased

### Current feature summary

- **Single local entry:** `startup.sh` now launches only the Universe API and the 5180 spatial room. The retired Vue page is no longer started or documented as a user entry; its source stays only for compatibility and contract tests.
- **Spatial Study room:** the monitor screen is the direct Study entry, the planning table remains the Plan/Review/Analytics entry, the bookshelf opens Knowledge/Wordbook, and the wall blackboard opens the card-and-note review space. The old large white hotspot outlines are removed; the bottom dock remains a shortcut.
- **Knowledge bookshelf:** uploaded documents render as physical books in the reference shelf composition, with three books per shelf page, subject filtering, multi-Goal association, edit/delete controls, a cover-click open step, paired-page reading, page jumping, local-first bookmarks and optional user/document progress synchronization. RAGFlow processing state remains visible and already-returned chunks stay readable.
- **Knowledge-to-Work authorization:** Goal-linked Study documents are absent from Work by default. A learner can grant and revoke an active Work Tech Stack's read-only access from the source reader, without duplicating the document, chunks or provider data.
- **Source-owned review:** selecting a passage creates a durable document-owned note or recall card. Cards support hidden-term recall, reveal and first-time `背过了` progress; a durable, explainable next-review schedule can be manually adjusted without moving card data into a separate store. Notes remain notes and do not enter the interval queue.
- **Wordbook review:** each tag is a vocabulary book using the same shelf/reader flow. Memory cards flip from English to the learner meaning, with durable remembered/mistake results, next-review dates/reasons and learner overrides while retaining the existing Wordbook fields.

### Fixed

- **2026-08-22 Work Case interaction recovery:** `/work` now owns a viewport-sized, independently scrollable container instead of being clipped by the spatial room's fixed 3D root. The Cases route and its native form controls remain editable even when the API is unavailable; persistence still truthfully requires the local PostgreSQL service to be running.
- **2026-08-20 Workbench runtime handoff:** `/work` in the active 5180 spatial entry now renders the locally deployed Sylva page instead of the retired 3D Work module; selecting Work Bench in the room follows the same route. The local-only Sylva source remains Git-ignored because its upstream README does not grant reuse or redistribution rights. Smoke validation passed: 213 backend tests (1 skipped), both frontend production builds, 4 bookshelf tests, API/Study/Wordbook/Knowledge/5180 proxy checks, Sylva asset delivery, and the non-mutating RAGFlow retrieval probe (`verified: true`).
- **2026-08-13 O4 source audit:** verified that the deployed `books-sigma-ashen` page exactly matches `thebuggeddev/books` main `index.html` and is a Three.js/WebGL implementation, not the Vite source placeholder. The upstream repository has no declared or detected license and no license file, so no reference source, asset or animation was copied into Universe; localizing the external shelf remains intentionally blocked pending explicit permission or a compatible upstream license.
- **2026-08-13 RAGFlow direct-upload bookshelf recovery:** added the controlled `POST /api/study/knowledge/documents/adopt-ragflow` bridge for an administrator-created RAGFlow file that had no Universe Knowledge record. It validates the exact provider dataset/document before persisting local ownership metadata, reuses an existing mapping on repeat calls, and immediately caches readable chunks. The route cannot upload content or submit a parse request, so it does not reset an in-flight RAGFlow queue.
- **2026-08-13 RAGFlow F1 runtime recovery:** normalized socket timeouts at the provider boundary so failed Universe-to-RAGFlow calls persist a truthful failed document state instead of remaining in parsing. New Goal/Tech Stack RAGFlow datasets now default to Plain Text with RAPTOR and GraphRAG disabled; this does not reparse existing data. A controlled local TXT/Markdown/PDF set completed with provider chunks, retrieval, Tutor source links and a 5180 reader check after recovery from one stuck new-PDF worker.
- **5180 blank room recovery:** diagnosed and restarted a stale Vite development process that had not reloaded the GLSL plugin configuration, causing shader source to be parsed as JavaScript. The 5180 room now mounts normally after the restart; the retired 5173 process remains stopped.

### Changed

- **2026-08-22 Work Case Foundation:** replaced the active `/work` Sylva landing page with a real React Practice Case workspace. `GET /api/work/home` now returns server-owned `activeCase`, `currentStage`, `nextAction` and `caseProgress`; Case list/create/read/update APIs persist through shared `work_records` with PostgreSQL/SQLite `case_id` migrations. Work Knowledge is retired from active UI navigation without deleting legacy records or APIs. Three.js Work Tree, BA/Artifacts, PM/Backlog, Governance, Labs, Operations, SOP, Radar and Bookshelf relations remain deliberately deferred.
- **2026-08-20 Room entry consolidation:** removed the Planning Table from the room and shortcut navigation while preserving the existing Plan, Review and Analytics direct routes. Renamed the visible entries to Study Space, 记忆卡片 and Work Space; shortcut navigation abbreviates Knowledge Library as “书架”. Removed the transient “正在装配书本场景” page so the Knowledge shelf renders its existing shelf directly when ready. No Study, Knowledge, Memory or Work data/API ownership changed.
- **2026-08-21 Study Space visual reuse:** `/study` and `/study/home` reuse the local Sylva immersion page. Study makes no Goal, task or primary-action request and adds no Study business interaction; the temporary static Study card and its generated cover were removed. The retired Study Home 3D scene and task crystals remain removed; Wordbook, Knowledge bookshelf, Study APIs, data models and AI behavior remain unchanged.
- **2026-08-13 F6 explainable Study feedback:** added `GET /api/study/feedback/recommendations`, a deterministic read-only Analytics endpoint. Each recommendation is traceable to an overdue review, the next incomplete Goal task or an optional synced reading position, includes its evidence and requires user confirmation. It neither calls the AI report path nor writes plans, tasks, review results, Learning Events, RAGFlow jobs or new tables. The existing Analytics room now surfaces the next recommendation as a cue only.
- **2026-08-13 F2 reading-progress synchronization:** added SQLite/PostgreSQL `reading_progress` migrations and `GET/PUT /api/study/knowledge/documents/{id}/reading-progress`. The reader now restores localStorage immediately, compares a returned server position, then saves locally before a best-effort API sync. The database upsert atomically rejects a stale client write and returns the server position instead of overwriting it. Reading position is not a Goal mastery, review result, content copy or RAGFlow action; deleting the source document clears only its progress.
- **2026-08-13 F5 multi-Goal Knowledge links:** added SQLite/PostgreSQL `document_goal_links` migrations with legacy `documents.goal_id` backfill. A Study document can now retain one primary `goalId` for existing provider dataset compatibility and attach to additional `goalIds` through explicit read/replace APIs and the bookshelf multi-select. Study listings and local/provider retrieval filter by the link table, so a secondary Goal can read the same document while unrelated Goals cannot. The source document, chunks, annotations and RAGFlow document are never copied or reprocessed. Work grants retain their explicit source Goal and remain valid only while that Goal link exists.
- **2026-08-13 F2 reading-progress design:** recorded the approved optional sync model in `docs/14_READING_PROGRESS_SYNC_DESIGN_2026-08-13.md`. It preserves the browser-local bookmark as immediate offline fallback, treats progress as one user/document record rather than a Goal fact, and defines last-write-wins conflict, deletion and privacy boundaries. No migration, API or reader behavior has been changed in this design-only slice.
- **2026-08-13 F4 Knowledge authorization:** added `knowledge_share_grants` SQLite/PostgreSQL migrations and source-owned grant APIs. Work Knowledge list/detail/home/Tech Stack evidence now expose only Work-owned documents or explicit active-Tech-Stack grants; granted Study documents cannot be processed, edited or deleted from Work. Revoking a grant, archiving a Tech Stack, removing that grant's explicit source-Goal link, or deleting the source document immediately removes Work visibility.
- **2026-08-13 F3 interval review V2:** added the shared `study_recall_schedules` persistence model and SQLite/PostgreSQL migrations for source-owned knowledge cards and Wordbook entries. The review API now exposes each item’s due date, rationale and manual adjustment, resets a forgotten item for immediate re-review, and makes identical same-day results idempotent so it cannot inflate word mistakes or Goal mastery. The physical reader renders and adjusts the same schedule; first-time Goal progress remains a separate fact layer.
- **2026-08-13 O5 Study Session transaction:** added a no-schema-change shared unit-of-work for the existing execution-finish API. Session conditional completion, linked Task completion, stable-ID Learning Event and the two stable-ID Memory facts now commit or roll back together; retried and concurrent requests return the same first finish without duplicate facts. SQLite covers every write-point failure and concurrency; PostgreSQL fault rollback/retry was verified in an isolated temporary schema that was deleted after the test, without touching active user data.
- **2026-08-13 O3 spatial style scoping:** moved the Knowledge bookshelf overlay/composer and Blackboard instrument styles out of the room shell into dedicated CSS modules. Existing visual values, responsive rules and z-index ordering remain unchanged; shared spatial layer tokens are now explicit in the root theme.
- **2026-08-13 O2 bookshelf responsibility split:** separated the physical shelf catalog, reader-page model and parent-side iframe protocol from `DeployedBooks.jsx`. The deployed reference HTML, visual motion, `postMessage` names and Universe API callbacks remain unchanged; new Node tests cover the pure catalog and page-model behavior.
- **2026-08-12 code and documentation audit:** centralized the spatial client's Study/Work Knowledge resource selection without changing API behavior; refreshed stale product, runtime, RAGFlow and implementation-history documentation; added a staged code-optimization and feature plan. The active bookshelf is now described accurately as a reference DOM/CSS 3D iframe scene, while the room remains Three.js.
- **2026-08-12 O1 spatial regression baseline:** added a dependency-free 5180 smoke script for the normal room routes and Vite API proxy, plus a route-contract test. It verifies delivery reachability separately from browser-level visual acceptance. Removed an unused monitor-hover state and corrected a blackboard selection-effect dependency so the spatial lint gate is clean.

### Historical delivery record (Git audited)

- **2026-07-07 — repository and Study foundations** (`110edf2`, `9bd0ae5`, `4c6bff5`, `8164c1f`, `1447172`): aligned the documentation-first repository structure; established the first Study milestone, learning workflow, AI Core/Tutor foundation, and the capability-oriented agent architecture.
- **2026-07-08 — Knowledge, retrieval, and memory foundations** (`1e312c8`, `f3c8943`, `213ca1d`, `85cdb79`, `570958f`, `bdb536b`): introduced the Knowledge document flow, retrieval and Tutor grounding path, scoped memory intelligence, Study intelligence, and completed the original Vite/TypeScript configuration.
- **2026-07-09 to 2026-07-10 — Study learning loop refinement** (`10bbcf3`, `a4007d4`, `f67a6a3`, `79654a8`, `ae4f55f`, `6a2f044`): delivered the initial product learning loop, local browser development support, improved goal/domain modeling and workspace information architecture, and documented the RAGFlow migration direction.
- **2026-07-22 — workspace stabilization and history consolidation** (`cbfb4f5`, `0018549`, `3c151bd`, `757c516`): stabilized the Study workspace and RAGFlow foundation, added productized goal creation and plan calendar work, and merged the pre-existing remote repository history without changing its provenance.
- **2026-07-23 — Work and shared Knowledge expansion** (`9794ad9`, `e0ac4b0`, `b2ede64`, `8c7efbc`, `cea7927`, `4c0d316`, `0b663a9`, `b1038c4`): added the Work Planet foundation, local service lifecycle scripts, technology-stack knowledge spaces, goal/stack-scoped Knowledge, articles, learning records, and related navigation and community-flow stabilization.
- **2026-07-24 — authoring, persistence, and evidence work** (`1b736fa`, `a5d7b13`, `9173467`, `025a7ad`, `04e55e4`, `6125505`, `3fd3234`, `9d5bdd7`, `02d9a12`, `8ca2098`, `efba51e`, `f3f3580`, `7d5f840`, `6035354`, `815a558`, `da1a6bf`): refined Work article reading/editing tools and home motion; added Study knowledge authoring, shared SQLite persistence, RAGFlow runtime-status handling, Tutor citation evidence, and the wrong-question review loop.
- **2026-07-25 — platform persistence and RAGFlow hardening** (`bdb4f48`, `04cf69d`, `e755a44`, `03e3795`): added the persistence/auth/plan base, plan builder and rich Knowledge authoring, restored the explicit Work article entry, and hardened scoped dataset and chunk-failure handling. These changes did not by themselves prove production completion of every RAGFlow document.
- **2026-07-26 — goal-aware Wordbook and Knowledge flow** (`4d9cc8c`, `ae581ed`, `872a41a`): polished the Study/RAGFlow Knowledge flow, added goal-aware Wordbook support, and repaired the Wordbook save path.
- **2026-08-08 — interactive physical bookshelf** (`3241ada`): integrated the Knowledge and Wordbook bookshelf experience, reader interactions, document-owned notes/cards, review state, and shelf navigation into the Study room.
- **2026-08-09 — spatial release integration and single entry** (`f41c815`, `1dd1079`, `f83c6c1`, `1d98abb`): integrated the current spatial room, Blackboard recall space, physical reader refinements, and the merged bookshelf work into `main`; retired the normal-user 5173 entry so `http://127.0.0.1:5180/` is the sole local UI entry.

### Added

- Knowledge Blackboard gallery treatment: the card field now adopts the hanging-rope gallery language from Rope Gallery—paired cords, brass pin anchors, dark exhibition board, inset paper stock and a restrained idle sway—while preserving Knowledge document ownership, reveal and mastery interactions.
- Knowledge Blackboard entrance: removed the Planning Table’s selected white room outline and reassigned the existing wall blackboard to `/study/cards`. The new spatial board reads existing document-owned annotations, separates knowledge cards from notes, supports hidden-term answer reveal and toggling “背过了” directly against the existing annotation API. Room model assets now use root-relative URLs so deep links such as `/study/cards` survive a browser refresh.
- Focus Reader and review cards: a physical open-book spread now keeps each generated paper page scroll-free, fitting content into additional pages before a turn. Learners can select a passage, choose a linked Goal, save it as a note or recall card, reveal randomly hidden key terms, and mark it “背过了”. Notes/cards remain attached to their Knowledge document through a durable annotation API and migration. Wordbook now offers an English-front / meaning-back memory card with remembered and mistake results. First-time mastery writes a Goal-linked Learning Event, so Study Workspace `progress.masteredItems` reports real recalled items without repeated-click inflation.
- Bookshelf reading and management polish: the original reference scene's `Bestsellers` mark is now the Knowledge/Wordbook switcher and its menu control is a compact room-home icon. A selected physical book opens only after its cover is clicked, then unfolds into the centered double-page reader; Wordbook pages now carry their real entry IDs so the reader can pronounce, edit and delete the displayed word. Study Knowledge supports title/subject/topic/goal edits and Wordbook supports durable entry deletion through its API contract. Shared room module routes now use shareable paths such as `/study/knowledge`, `/study/wordbook` and `/novel`; pronunciation prefers a natural installed English voice before falling back to the browser default.
- Study Wordbook bookshelf: tags now map one-to-one to the same deployed reference DOM/CSS 3D book scene used by Knowledge. A vocabulary book retains the source project's physical open/detail motion; clicking the opened physical cover then swings it around its spine into a two-page paper spread, with double-page turns for word, pronunciation, meaning, phrases, examples and personal notes. The source scene's floating Open slip is now a direct reading control and allows longer trackpad/touch taps, preventing slow clicks from being discarded before opening. Wordbook retains its own cached API recovery state, language grouping, shelf pagination, a Knowledge return path and in-scene single-word creation.
- Study bookshelf module: the Knowledge entry loads the deployed `books-sigma-ashen` reference HTML into a controlled iframe, maps existing document metadata into its procedural green, vermilion and blue cover designs, and preserves the reference scene's DOM/CSS physical cover/detail/open-close motion. Opening a book reads its real Universe document chunks into the reference detail panel with page controls; documents still processing in RAGFlow explicitly show their provider status instead of an empty reader. The source scene retains its three-book hero composition per shelf page, with in-scene previous/next shelf controls once a collection exceeds three books, subject filtering, goal selection during upload, confirmed Study Knowledge deletion, a return path to Wordbook, and explicit API-unavailable plus cached-metadata behavior.
- English-English Dictionary reference: Study Knowledge now exposes a shared dictionary document. Manual or TXT/CSV-imported English words automatically attach dictionary status, pronunciation and usage records through a replaceable Knowledge-side provider, while personal meanings, phrases, examples and notes remain independently editable. The 2D and spatial Wordbook both expose a manual reference refresh action and show truthful unavailable/not-found states.
- Spatial Wordbook IME input fix: single-word creation now keeps text fields native and uncontrolled while typing, reads values once on submit, and stops composition/keyboard events from reaching the 3D controls so Chinese Pinyin composition is not interrupted.
- PostgreSQL runtime recovery: the shared persistence adapter now reconnects closed sessions before the next operation and retries only safe read statements after a dropped connection; Wordbook creation surfaces visible loading, success and database-unavailable states instead of appearing inactive.

- Spatial Wordbook redesign (historical, superseded by the physical vocabulary bookshelf): the former floating card grid and unrelated crystal detail were replaced by a searchable, language/tag-filtered vocabulary garden with eight plants per page. Words remained selectable 3D plants, the selected entry opened a readable physical lexicon stand, and in-world single-word creation plus TXT/CSV import instruments used the existing Wordbook API.

- Spatial room portal navigation: Study modules now enter through the learning computer, Plan/Review/Analytics through the planning table, Knowledge/Wordbook through the bookshelf, and Work/Novel through their room objects. Each portal first focuses its furniture and then opens a nearby 3D module world instead of flying to the former remote `z=-50` stage; room and module docks remain clickable above in-world HTML.

- Spatial Universe room: the original GLB wall and floor geometry now extend into a larger continuous room with a desk-matched Knowledge bookshelf, Work bench and novel exhibition wall. The former central web panel has been removed. Fourteen Study, Work and Novel modules now express live API data through physical 3D objects in the original room material language; Study Plan uses a Blender-authored orbital calendar with six week rings, 42 clickable date nodes, a Goal core and a rising task ribbon. Knowledge opens books from a shelf, and Tutor, Goals, Review, Analytics, Work and Novel use dedicated spatial instruments.

- Product smoke-test reliability fixes: PostgreSQL repositories now tolerate native JSONB/date values and legacy normalized table requirements; session completion repairs idempotently after a transient failure; Work Knowledge now refreshes RAGFlow status instead of reissuing parse requests while a document is running.

- Milestone 1 foundation backend structure, API contracts, Planet registry, Memory scope boundary, database foundation, and Study Workspace frontend shell.
- Milestone 2 Study learning workflow foundation: Goal, Plan, Daily Tasks, Study Sessions, Study Home progress aggregation, database migration, frontend Plan/Home workflow, and tests.
- Milestone 3 AI Core and Study Tutor foundation: shared AI Core entry point, deterministic LLM Gateway, Prompt/Context/Agent managers, Study Tutor consumer, Learning Events, Tutor API/frontend, and tests without RAG or embeddings.
- Milestone 3.5 AI Core generalization: AgentDefinition registry, context providers, prompt keys, provider-only LLM Gateway boundary, and tool interface preparation without RAG implementation.
- Milestone 4.1 Knowledge foundation: shared Knowledge service, File service foundation, document/chunk/concept models, database migration, Knowledge API/frontend, and tests without embeddings, vector storage, or Tutor integration.
- Milestone 4.2 Retrieval foundation: embedding provider abstraction, deterministic embedding provider, vector store interface, in-memory test store, chunk embedding pipeline, retrieval API, and tests without Tutor or RAG integration.
- Milestone 4.3 grounded Tutor foundation: AI Core ToolRouter invocation, RetrieverTool adapter, Knowledge context injection, grounded Tutor metadata, frontend grounding display, and tests without Knowledge Graph or new Agents.
- Milestone 5 Memory Intelligence foundation: shared Memory Manager, lifecycle/status fields, scoped retrieval, AI context preparation, Memory API, Tutor memory context payload, and tests without autonomous extraction or personality inference.
- Milestone 6 Study Intelligence foundation: Study Agent Analyst capability, Analytics service, progress metrics, structured reports, Analytics frontend, and tests without autonomous decisions, new Agents, or new Planets.
- Milestone 7 Study product learning loop: Study onboarding, manual plan productization, Study Home control center, session execution wrapper, factual Memory write points, Analytics insight integration, frontend loop screens, and tests without AI Core redesign or new Planets.
- Local browser run chain: Vite dev server now proxies `/api` to the local FastAPI backend and README documents the two-terminal startup flow.
- Study workspace experience update: Universe return navigation, configurable exam/learning/reading/growth goals, optional deadlines, real Knowledge file selection for txt/markdown/PDF metadata, and Home response aliases for progress, Knowledge overview and Analytics insight.
- Milestone 7.5 Study domain model refinement: reading goals, multiple goals with switching, multiple plans per goal, plan type separation, optional Knowledge goal links, normalized migration numbering, and compatibility tests without AI Core or RAG changes.
- Milestone 7.6 Study product workspace UX: Study Workspace aggregation API, Goal management page, current-Goal Home control center, guided Plan tree UI, Knowledge Goal filtering, and tests without AI Core, RAG, Memory, or Knowledge Graph expansion.
- Milestone 7.8.1 Study Workspace IA refactor: Current Goal header context, six-item Study navigation, Home primary-action hierarchy, single Plan Structure creation action, Knowledge upload enablement, workspace `planSummary`, and IA tests without AI Core, Retrieval, Memory, or database changes.
- Study Workspace smoke-test fixes: backend-owned `primaryAction`, completed-task action cleanup, PDF metadata status copy, Tutor empty-input validation, compact Goal switching, Goals IA grouping, human-readable Analytics language, and Review coming-later copy.
- Milestone 8.1 RAGFlow Knowledge provider adapter: provider config, RAGFlow upload/parse/chunk/retrieval client, provider metadata migration, backend Knowledge/ Retrieval routing, frontend provider status display, and mocked adapter tests while preserving local fallback.
- Local RAGFlow runtime stack: project-owned Docker Compose service under `docker/ragflow/`, local Universe env example, start/stop scripts, and installation guide.
- Milestone 7.9 Study productization slice: drilled Create Goal flow, Goal-type Knowledge Space preview, Study Plan Calendar, Daily Task priority, migration `010_study_plan_calendar_priority.sql`, and IA regression tests.
- Milestone 10.0 Work Planet foundation: active Work Planet entry, Work Workspace, Tech Stack directory/detail, project evidence records, Dynamic Resume drafts, Work APIs, migration `011_work_planet_foundation.sql`, and tests without a new AI system or Work Agent.
- Work Planet IA refinement: Work Knowledge is now a first-class workspace module, while Study Knowledge is treated as reference material through the shared Knowledge Service.
- Knowledge Space metadata refinement: documents now support Planet scope, Work Tech Stack binding and tags, while RAGFlow uses separate datasets for different Study Goals.
- Work Tech Stack content refinement: Tech Stacks now support owned articles and learning records, with directory-level content feed and resume evidence refs.
- Work Tech Stack navigation refinement: stack tabs now use actual Tech Stack names, Community tab shows CSDN public articles, and Work articles are typed as Knowledge or Note.
- Work Tech Stack directory workflow fix: Add Tech Stack now opens a modal, directory navigation uses a two-row category/stack structure, and the CSDN Community tab keeps 30 displayable discovery items when live fetch is unavailable.
- Work Tech Stack directory visual refinement: selected category tabs now use the system teal colorway, and the duplicated left-side Tech Stack directory card was removed after the second-row stack selector.
- Work Tech Stack directory context cleanup: the redundant `Open Stack` summary card was removed because selecting the second-row stack tab already enters that stack context.
- Universe Home visual refinement: the portal starfield now uses alternating vertical motion layers so adjacent dot columns drift in opposite directions.
- Study Knowledge authoring refinement: Study Knowledge now includes a Write Article mode that saves goal-linked markdown articles into Knowledge and processes them into chunks through the existing Knowledge API.
- Work Community reading fix: CSDN articles can now expand inline inside Work Planet through a backend article detail endpoint, and Community loading no longer blocks switching back to `全部` or other directories.
- Work Tech Stack authoring refinement: Tech Stack detail now supports editing/archiving stacks and a focused article editor with an auto-generated left outline plus a compact title-level toolbar that inserts image, editable table and code content directly into the article body with basic text formatting and cell merge/split.
- Work article entry refinement: Work Home, Tech Stack directory and Tech Stack detail now expose an explicit `写文章` entry; stack detail defaults to the article library and only opens the rich editor after an intentional action.
- Shared persistence foundation: added a migration runner, shared SQLite database, repository adapters for Study, Knowledge, Memory and Work, durable `user_planet_context`, and restart integration tests. Direct test facades remain isolated in-memory by default.
- RAGFlow runtime contract foundation: added provider health checks, asynchronous document status refresh, frontend polling, retry handling, and provider deletion synchronization. Local runtime health is confirmed, while real document processing remains blocked by the configured RAGFlow embedding provider returning `InvalidApiKey`; no credential is stored in the repository.
- Four-stage product loop completion: shared current-Goal SQLite persistence, unified Tutor Evidence sources with scope/quote preview/save-as-Learning-Event, and factual Wrong Question -> 1/3/7/30 Review -> Analytics flow. Real RAGFlow processed-file acceptance remains blocked by the external embedding provider configuration.
- Platform reliability and workflow completion: PostgreSQL repository adapter/migration runner, local/S3 object storage boundary, backup/restore scripts, email-verification registration, Goal-owned Plan Builder nodes with task ordering, and Study Knowledge rich article editing.
- RAGFlow configuration diagnostics now expose optional Universe-side embedding/LLM/rerank labels without moving provider logic into AI Core. Real processed-file acceptance remains dependent on RAGFlow runtime validation.
- RAGFlow scope handling now resolves readable, isolated dataset names from Study Goal and Work Tech Stack records, reuses exact existing datasets, and exposes actionable embedding-provider error codes for failed processing.
- RAGFlow-backed Knowledge and Tutor polish: provider PDF uploads now enter the same preparation loop as TXT/Markdown, retrieval accepts only processed documents in the active Universe scope, and Knowledge/Tutor present lifecycle status plus exact-passage Evidence navigation.
- Study Wordbook: added durable Goal-aware vocabulary entries, language and tag filters, manual and TXT/CSV bulk import, duplicate handling, word-detail tags/phrases/example sentences/notes, Study API contracts, and a focused Study Workspace route.
- Wordbook save and list reliability: repaired legacy Wordbook language migrations, return saved words to their language/scope view, and surface backend errors instead of leaving an empty list.
- PostgreSQL runtime baseline: PostgreSQL is now the default backend persistence configuration, with a local pgvector Compose profile and a forward-compatible Wordbook migration; SQLite remains an explicit development/test adapter.
- PostgreSQL JSONB compatibility: shared repository decoding now accepts native JSONB objects, restoring Wordbook create, detail save and list reads through the live API.
- Universe visual workspace refactor: Portal, Study Planet, and Work Planet now share a focused deep-space visual system with accessible planet entry points, compact breadcrumb navigation, data-preserving module rails, and reduced-motion-aware moving stars and meteors.
